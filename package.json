{
  "name": "telegram-ai-bot",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "check": "node --check index.js && node --check src/app.js && node --check src/bot.js",
    "test": "node --test",
    "test:watch": "node --test --watch"
  },
  "keywords": [
    "telegram-bot",
    "openai-api",
    "webhook",
    "automation"
  ],
  "description": "Secure webhook-based Telegram bot integrating OpenAI, weather data and text-to-speech with Node.js and Express.",
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "axios": "^1.19.0",
    "dotenv": "^16.6.1",
    "express": "^4.22.2",
    "openai": "^4.104.0",
    "telegraf": "^4.16.3"
  },
  "devDependencies": {
    "nodemon": "^3.1.14"
  }
}
